<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  事务发件箱 MyBatis 实现。

  说明：
  1. published_at 省略（未发布即 NULL，分发器发布后回填）；
  2. trace_id 为 V7 新增列，创建入口写入以便异步链路延续 trace 上下文。
-->
<mapper namespace="com.example.exportflow.export.mapper.OutboxEventMapper">

    <!-- 插入 Outbox 事件（与导出任务同事务写入，无需回填主键）。 -->
    <insert id="insert">
        INSERT INTO outbox_events (
            aggregate_type, aggregate_id, event_type,
            payload, trace_id, created_at
        ) VALUES (
            #{aggregateType}, #{aggregateId}, #{eventType},
            #{payload}, #{traceId}, #{createdAt}
        )
    </insert>
</mapper>
