<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  事务发件箱 MyBatis 实现。

  说明：
  1. published_at 省略（未发布即 NULL，分发器发布后回填）；
  2. trace_id 为 V7 新增列，创建入口写入以便异步链路延续 trace 上下文。
-->
<mapper namespace="com.example.flowhub.export.mapper.OutboxEventMapper">

    <!-- 插入 Outbox 事件（与导出任务同事务写入，无需回填主键）。 -->
    <insert id="insert">
        INSERT INTO outbox_events (
            aggregate_type, aggregate_id, event_type,
            payload, trace_id, created_at
        ) VALUES (
            #{aggregateType}, #{aggregateId}, #{eventType},
            #{payload}, #{traceId}, #{createdAt}
        )
    </insert>

    <!-- 捞取未发布事件（published_at IS NULL 走 idx_outbox_unpublished 前缀；LIMIT 参数化，MySQL/H2 均支持）。 -->
    <select id="findUnpublished" resultType="com.example.flowhub.export.entity.OutboxEventEntity">
        SELECT
            id,
            aggregate_type AS aggregateType,
            aggregate_id   AS aggregateId,
            event_type     AS eventType,
            payload,
            trace_id       AS traceId,
            created_at     AS createdAt
        FROM outbox_events
        WHERE published_at IS NULL
          AND aggregate_type = #{aggregateType}
          AND event_type = #{eventType}
        ORDER BY created_at, id
        LIMIT #{limit}
    </select>

    <!-- 标记已发布：published_at IS NULL 单向条件保证不可逆，重复调度重放安全；时间由应用侧传入。 -->
    <update id="markPublished">
        UPDATE outbox_events
        SET published_at = #{publishedAt}
        WHERE id = #{id}
          AND published_at IS NULL
    </update>
</mapper>
